import Page from "./page";

export const metadata = {
  title: "Monetization",
  description: "Monetization Form",
};
export default async function RootLayout({ children }) {
  return <div>{children}</div>;
}
